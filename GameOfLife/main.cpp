#include <iostream>
#include <vector>

void InitGrid(std::vector<char>grid);

int main()
{
    std::vector<char> vec1;
    vec1.push_back('f');

    std::cout << "Hello World!\n" << vec1.at(0);
}

// Have a straight vec that has all the numbers in it. but when pasted, split it up into lines?
// so store it as x,x,x,x,x,x,x,x,x,x,x,x
// then cout it as
/*
x,x,x
x,x,x
x,x,x
x,x,x
*/

void InitGrid(std::vector<char>grid)
{

}
